# Contributing

Use Node 22+, run `npm install`, and validate changes with `npm run check` and `npm run test:e2e`. Keep gameplay deterministic, UI keyboard-accessible, and all media original. Regulatory text must cite an official source, include a verification date, and never encode real limits as durable game rules. Open focused pull requests with tests and screenshots for visible changes.
