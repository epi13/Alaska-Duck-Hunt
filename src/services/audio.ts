export class AudioManager { private ctx?:AudioContext; muted=false; volume=.65; async unlock(){this.ctx??=new AudioContext();await this.ctx.resume()} tone(freq:number,duration=.08,type:OscillatorType='square',gain=.05){if(this.muted||!this.ctx)return;const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(gain*this.volume,this.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,this.ctx.currentTime+duration);o.connect(g).connect(this.ctx.destination);o.start();o.stop(this.ctx.currentTime+duration)} shot(){this.tone(95,.12,'sawtooth',.12)} nav(){this.tone(440,.04)} success(){[523,659,784].forEach((f,i)=>setTimeout(()=>this.tone(f,.18),i*110))} }
